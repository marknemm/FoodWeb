import { ReturnLinkDirective } from './return-link.directive';

describe('ReturnLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ReturnLinkDirective();
    expect(directive).toBeTruthy();
  });
});
