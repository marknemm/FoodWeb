import { FragmentAccordianDirective } from './fragment-accordian.directive';

describe('FragmentAccordianDirective', () => {
  it('should create an instance', () => {
    const directive = new FragmentAccordianDirective();
    expect(directive).toBeTruthy();
  });
});
