import { DisplayEditTransitionDirective } from './display-edit-transition.directive';

describe('DisplayEditTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayEditTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
