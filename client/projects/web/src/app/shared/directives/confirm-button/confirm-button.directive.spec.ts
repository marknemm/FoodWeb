import { ConfirmButtonDirective } from './confirm-button.directive';

describe('ConfirmButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmButtonDirective(null);
    expect(directive).toBeTruthy();
  });
});
