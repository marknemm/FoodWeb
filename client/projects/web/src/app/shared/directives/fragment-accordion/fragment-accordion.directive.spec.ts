import { FragmentAccordionDirective } from './fragment-accordion.directive';

describe('FragmentAccordionDirective', () => {
  it('should create an instance', () => {
    const directive = new FragmentAccordionDirective();
    expect(directive).toBeTruthy();
  });
});
