import { ControlNameDirective } from './control-name.directive';

describe('ControlNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlNameDirective();
    expect(directive).toBeTruthy();
  });
});
