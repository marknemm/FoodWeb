import { FormatEventLocationPipe } from './format-event-location.pipe';

describe('FormatEventLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEventLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
