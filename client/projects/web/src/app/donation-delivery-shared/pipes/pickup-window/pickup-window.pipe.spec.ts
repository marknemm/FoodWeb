import { PickupWindowPipe } from './pickup-window.pipe';

describe('PickupWindowPipe', () => {
  it('create an instance', () => {
    const pipe = new PickupWindowPipe();
    expect(pipe).toBeTruthy();
  });
});
