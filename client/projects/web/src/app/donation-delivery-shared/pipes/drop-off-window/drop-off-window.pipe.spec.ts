import { DropOffWindowPipe } from './drop-off-window.pipe';

describe('DropOffWindowPipe', () => {
  it('create an instance', () => {
    const pipe = new DropOffWindowPipe();
    expect(pipe).toBeTruthy();
  });
});
