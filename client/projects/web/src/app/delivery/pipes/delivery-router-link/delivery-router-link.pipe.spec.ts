import { DeliveryRouterLinkPipe } from './delivery-router-link.pipe';

describe('DeliveryRouterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryRouterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
