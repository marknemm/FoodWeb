import { AppVisibleDirective } from './app-visible.directive';

describe('AppVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
