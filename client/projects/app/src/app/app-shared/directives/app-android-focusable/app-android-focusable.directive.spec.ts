import { AppAndroidFocusableDirective } from './app-android-focusable.directive';

describe('AppAndroidFocusableDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAndroidFocusableDirective();
    expect(directive).toBeTruthy();
  });
});
