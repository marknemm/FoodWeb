import { ListPageDirective } from './list-page.directive';

describe('ListPageDirective', () => {
  it('should create an instance', () => {
    const directive = new ListPageDirective();
    expect(directive).toBeTruthy();
  });
});
