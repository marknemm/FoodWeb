import { IonMaskDirective } from './ion-mask.directive';

describe('IonMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new IonMaskDirective();
    expect(directive).toBeTruthy();
  });
});
